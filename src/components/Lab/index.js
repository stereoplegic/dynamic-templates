import React from 'react';
// import { } from 'prop-types';

const Lab = (props) => {
    return (
        <section>
            {/* add component text here */}
        </section>
    )
};

Lab.propTypes = {

};

export default Lab;
